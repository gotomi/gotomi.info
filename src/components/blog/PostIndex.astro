---

export interface Props {
  posts: any,
  title: any,
}
const { posts, title} = Astro.props;

import PostPreview from '../../components/blog/PostPreview.astro'

---


<h2 class="section-title">{title}</h2>
  {posts.map((post) => (
    <PostPreview slug={post.slug}/>
  ))}