{
  "name": "gotomi_info",
  "version": "1.0.0",
  "description": "gotomi.info homepage",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cp -r src/assets dist/assets && cp src/favicon.ico dist && jade src/index.jade -o dist"
  },
  "author": "Tomasz Gospodarczyk",
  "license": "ISC",
  "devDependencies": {
    "jade": "^1.11.0"
  }
}
